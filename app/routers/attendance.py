"""Attendance routes — School ERP (Phase 1 scaffolding).

Routes will be implemented in a later phase. For now this exposes an empty
router so the package structure is in place and it can be registered in
main.py without affecting the running app.
"""
from fastapi import APIRouter

router = APIRouter()
